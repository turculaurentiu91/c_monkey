{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "make parser_test",
            "type": "shell",
            "command": "make clean && make all",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}